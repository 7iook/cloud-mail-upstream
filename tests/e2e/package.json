{
  "name": "cloud-mail-share-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser harness for the Mail Share visitor flow. One command from the repo root: node tests/e2e/run.mjs",
  "scripts": {
    "test": "node run.mjs",
    "test:headed": "node run.mjs --headed",
    "playwright": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
